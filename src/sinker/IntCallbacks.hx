package sinker;

class IntCallbacks {
	/**
		@see `Ints.square()`
	**/
	public static final square = (value: Int) -> Ints.square(value);

	/**
		@see `Ints.absolute()`
	**/
	public static final absolute = (value: Int) -> Ints.absolute(value);
}
